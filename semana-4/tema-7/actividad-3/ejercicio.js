// Parte 1
var str = 'Copa de fútbol'

str.includes('Copa') // ?
str.includes('Torneo') // ?

// Parte 2
class miClass {
    constructor(a, b) {
        this.result = a + b
    }
}

const miFunción = (a, b) => a + b
const resultado = [1, 2].reduce((a, b) => a + b, 0)
